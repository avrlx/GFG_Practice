# Palindrome String

**Difficulty:** Easy

**GeeksforGeeks:** [View Problem](https://www.geeksforgeeks.org/problems/palindrome-string0817/1)

**Topics:** Strings, two-pointer-algorithm

## Problem Statement

Given a string **s**, return **true** if the string is a **palindrome**. Otherwise, return **false**.

A string is considered a palindrome if it reads the same forwards and backwards.

**Examples :**

```text
Input: s = "abba"
Output: true
Explanation: "abba" reads the same forwards and backwards, so it is a palindrome.
```

```text
Input: s = "abc"
Output: false
Explanation: "abc" does not read the same forwards and backwards, so it is not a palindrome.
```

**Constraints:**
1 &le; s.size() &le; 10^6
The string `s` contains only lowercase english letters (a-z).
